package com.alidev.android.portfolio.data

interface CameraRepository {
    fun openCamera()
    class Impl() : CameraRepository {
        override fun openCamera() {
            TODO("Not yet implemented")
        }
    }
}